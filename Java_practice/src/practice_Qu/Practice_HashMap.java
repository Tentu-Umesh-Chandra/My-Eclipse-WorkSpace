package practice_Qu;
import java.util.*;
public class Practice_HashMap {
	public static void main(String[] args) {
		// TODO Auto-generated method stub
		Scanner sc = new Scanner(System.in);
		/*
		1. in String find the character in that string and find all the occurance of char and replace
		if char is not present in the string +"" 
		replace the only first occurance. 
		take input of char and then convert it into string use .matches() 
		2. size of string should'nt be less than 2 if str.matches("[a-zA-Z]+") 
		3. EVCharging- create 4 parameterized constructors; calculate method returns double takes 2 parameters 
		not part of above 4 
		4. Delivery/Dining- 4clases inheritance(1 parent(Service),userInterface, 2 child(restaurant,delivery)). In childs use 
		Super class to extend the parent. in restaurant class return type double and give discount
		delivery class has 6 parameters and 4 from parent. don't modify userInterface.
		5. Consultancy has HashMap in HMap method <>
		filter method take Doublevalue as parameter add key in ArrayList returns double
		don't modify userInterface
		6. Styling- Using HashSet; Don't modify userInterface
		cloathing details separated by ":" 
		min and max quantity
		hoodie-large:100
		
		*/
	}

}
